apiVersion: v1
kind: Service
metadata:
  name: nuc-chart-mcp
  namespace: nuc-chart-mcp
  labels:
    app.kubernetes.io/name: nuc-chart-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nuc-chart-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
